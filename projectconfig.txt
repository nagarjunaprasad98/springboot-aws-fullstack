run this command in aws Linux
sudo yum install java-21-amazon-corretto-devel -y


echo 'export JAVA_HOME=$(dirname $(dirname $(readlink -f /usr/bin/java)))' | sudo tee /etc/profile.d/java_home.sh > /dev/null
echo 'export PATH=$PATH:$JAVA_HOME/bin' | sudo tee -a /etc/profile.d/java_home.sh > /dev/null

sudo chmod +x /etc/profile.d/java_home.sh

source /etc/profile.d/java_home.sh

after we get java -version

wget https://raw.githubusercontent.com/nagarjunaprasad98/aws_jars_wars/refs/heads/main/sb1-0.0.1-SNAPSHOT.war

after jar is downloaded

java -jar sb1-0.0.1-SNAPSHOT.war &

after applicaito running

wget http://localhost:8081/

index.htmlis generated

less index.html